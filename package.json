{
  "name": "@guaclive/verify-patrons",
  "version": "1.0.2",
  "description": "verify if patrons are actually patrons, if not patch them.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2"
  }
}
